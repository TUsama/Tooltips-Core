### V0.1.0
rewrite core function code
fix a bug that the dashed line would be connected incorrectly if two same term appear in a same component.
fix a potential memory leak bug that cause by dashed line connecting

### V0.0.4
fix 1.21.1

### V0.0.3
rework dashed line render.